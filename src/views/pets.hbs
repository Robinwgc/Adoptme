<!DOCTYPE html>
<html>
<head>
    <title>🐶 Mascotas - Adopt Me</title>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; background: #f5f5f5; padding: 20px; }
        .container { max-width: 900px; margin: 0 auto; background: white; padding: 40px; border-radius: 15px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
        h1 { color: #333; margin-bottom: 30px; text-align: center; }
        .pet-card { 
            padding: 20px; 
            margin: 15px 0; 
            border-radius: 12px; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        .pet-card:hover { transform: translateY(-3px); }
        .common { background: linear-gradient(135deg, #e8f5e8, #c8e6c9); border-left: 5px solid #4caf50; }
        .rare { background: linear-gradient(135deg, #e0f2fe, #b3e5fc); border-left: 5px solid #2196f3; }
        .legendary { background: linear-gradient(135deg, #fff3e0, #ffcc80); border-left: 5px solid #ff9800; }
        .pet-name { font-size: 1.5em; font-weight: bold; color: #333; }
        .pet-rarity { 
            display: inline-block; 
            padding: 5px 12px; 
            border-radius: 20px; 
            font-size: 0.9em; 
            font-weight: bold;
            margin-left: 10px;
        }
        .common .pet-rarity { background: #4caf50; color: white; }
        .rare .pet-rarity { background: #2196f3; color: white; }
        .legendary .pet-rarity { background: #ff9800; color: white; }
        .pet-owner { font-size: 0.95em; color: #666; margin-top: 5px; }
        .empty { text-align: center; color: #666; font-size: 1.2em; margin: 50px 0; }
        .back-btn { 
            display: inline-block; 
            background: linear-gradient(45deg, #ff6b6b, #feca57); 
            color: white; 
            padding: 12px 25px; 
            text-decoration: none; 
            border-radius: 25px; 
            margin-top: 20px; 
            transition: all 0.3s ease;
        }
        .back-btn:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(0,0,0,0.2); }
    </style>
</head>
<body>
    <div class="container">
        <h1>🐶 Todas las Mascotas Registradas</h1>
        
        {{#if error}}
            <div style="background: #ff6b6b; color: white; padding: 15px; border-radius: 8px; margin: 20px 0;">{{error}}</div>
        {{/if}}
        
        {{#if pets.length}}
            {{#each pets}}
                <div class="pet-card {{this.rarity}}">
                    <div class="pet-name">🐾 {{this.name}}</div>
                    <span class="pet-rarity">{{this.rarity}}</span>
                    {{#if this.owner}}
                        <div class="pet-owner">👤 Dueño: {{this.owner}}</div>
                    {{/if}}
                </div>
            {{/each}}
        {{else}}
            <div class="empty">
                ❌ No hay mascotas registradas aún<br>
                <small>¡Crea la primera desde la API POST /api/pets!</small>
            </div>
        {{/if}}
        
        <a href="/" class="back-btn">← Volver al Inicio</a>
    </div>
</body>
</html>
